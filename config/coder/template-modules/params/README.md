# Shared Template Modules (Terraform glue)
# Location moved inside templates/ to align with template-centric structure.
# These module-*.tf files are overlaid into each template during push.
# Precedence: template-local file overrides shared file of same name.
