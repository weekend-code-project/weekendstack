# =============================================================================
# SSH Parameters (Vite Template Override)
# =============================================================================
# OVERRIDE NOTE: SSH completely disabled for Vite template (not needed for web development)

# No SSH parameters - module completely disabled
# This prevents the shared ssh-params.tf from loading
