# =============================================================================
# Docker Parameters (Vite Template Override)
# =============================================================================
# OVERRIDE NOTE: This file overrides the shared docker-params.tf
# Docker-in-Docker is completely disabled for Vite templates (not needed for frontend dev)

# No docker parameter - module completely disabled
# No module "docker" block - prevents UI toggle from appearing
