# =============================================================================
# Setup Server Parameters - WordPress Override
# =============================================================================
# WordPress template doesn't use setup-server module (has its own Apache setup)
# This file prevents overlay of setup-server-params.tf from shared params

# No setup-server module needed for WordPress template
