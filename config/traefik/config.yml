# Traefik v3 Static Configuration
# Entrypoints and Docker provider are declared in docker-compose command args.
# This file enables the file provider so dynamic configs (TLS, auth) are loaded.
providers:
  file:
    directory: /config/traefik/auth
    watch: true
